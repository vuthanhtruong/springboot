{
  "version": 2,
  "builds": [
    {
      "src": "pom.xml",
      "use": "vercel-bash",
      "config": {
        "command": "mvn clean package && cp target/*.jar ."
      }
    }
  ],
  "routes": [
    {
      "src": "/hello/(.*)",
      "dest": "/api/hello"
    }
  ]
}